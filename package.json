{
  "name": "woohannahdotcom",
  "description": "Hannah Woo - Artist Portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd site && yarn dev",
    "build": "cd site && yarn build",
    "preview": "cd site && yarn preview",
    "go": "yarn build && git add . && git commit -m \"Update website\" && git push origin main",
    "deploy": "yarn build && git add . && git commit -m \"Deploy website\" && git push origin main",
    "format": "prettier . --write"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
